machine:
  node:
    version: 6.1.0

dependencies:
  post:
    - npm install -g gulp-cli && gulp

deployment:
  production:
    branch: master
    commands:
      - gulp publish --target prod
      - gulp publish --target dev
